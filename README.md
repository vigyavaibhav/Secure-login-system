# Secure-login-system
Secure Login System is a Flask-based web application that provides user registration and authentication using bcrypt password hashing, SQLite, session management, and SQLAlchemy for SQL injection protection. It ensures secure access with login, logout, and input validation.
